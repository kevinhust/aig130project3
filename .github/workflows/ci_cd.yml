name: CI/CD Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

env:
  PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}
  GAR_LOCATION: us-central1
  REPOSITORY: aig130-repo
  IMAGE: aig130-project3

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Lint with flake8
      run: |
        # stop the build if there are Python syntax errors or undefined names
        flake8 src --count --select=E9,F63,F7,F82 --show-source --statistics
        # exit-zero treats all errors as warnings.
        flake8 src --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics

    # Uncomment this when you have actual tests
    # - name: Test with pytest
    #   run: |
    #     pytest

  # Optional: Build and Push Docker Image
  # This requires configuring GCP Workload Identity Federation or Service Account secrets
  # build-push:
  #   needs: test
  #   runs-on: ubuntu-latest
  #   steps:
  #   - name: Checkout
  #     uses: actions/checkout@v3
  #   
  #   - name: Google Auth
  #     uses: google-github-actions/auth@v1
  #     with:
  #       credentials_json: '${{ secrets.GCP_SA_KEY }}'
  #
  #   - name: Set up Cloud SDK
  #     uses: google-github-actions/setup-gcloud@v1
  #
  #   - name: Build and Push to Artifact Registry
  #     run: |
  #       gcloud auth configure-docker ${{ env.GAR_LOCATION }}-docker.pkg.dev
  #       docker build -t ${{ env.GAR_LOCATION }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.REPOSITORY }}/${{ env.IMAGE }}:${{ github.sha }} .
  #       docker push ${{ env.GAR_LOCATION }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.REPOSITORY }}/${{ env.IMAGE }}:${{ github.sha }}
